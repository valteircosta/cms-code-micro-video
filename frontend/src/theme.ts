import { createMuiTheme } from "@material-ui/core";


const theme = createMuiTheme({
    palette: {
        primary: {
            main: '#79aec8',
            contrastText: '#ffff'
        },   
        secondary: {
            main: '#4db5ab',
            contrastText: '#ffff'
        }   
 }
});

export default theme;
